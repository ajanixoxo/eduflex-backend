export enum UserTypes {
  CUSTOMER = 'customer',
  STAFF = 'staff',
}

export enum UserStatus {
  NEW = 'new',
  VERIFIED = 'verified',
  INACTIVE = 'inactive',
  BANNED = 'banned',
  DELETED = 'deleted',
}

export enum UserRoles {
  MASTER = 'master',
  ADMIN = 'admin',
  SUPPORT = 'support',
  SALES = 'sales',
  FINANCE = 'finance',
  REGULAR = 'regular',
}

export enum UserPlans {
  FREE = 'free',
  PRO = 'pro',
}
